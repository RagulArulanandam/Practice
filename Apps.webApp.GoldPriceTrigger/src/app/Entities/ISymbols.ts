export interface ISymbols {
    name: string;
    symbols: string;
}

export class PriceDetails {
    name: string;
    price: number;
    symbol: string;
    updatedAt: string;
    updatedAtReadable: string;
}